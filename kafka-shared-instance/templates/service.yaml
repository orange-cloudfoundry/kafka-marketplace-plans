apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-shared-instance.fullname" . }}
  labels:
    {{- include "kafka-shared-instance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-shared-instance.selectorLabels" . | nindent 4 }}
